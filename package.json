{
	"name": "enya",
	"module": "src/index.ts",
	"devDependencies": {
		"@biomejs/biome": "1.9.3",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	},
	"description": "An ergonomic env parsing library",
	"scripts": {
		"test": "bun run index.test.ts",
		"test:watch": "bun run --watch index.test.ts",
		"format": "bunx biome check --write .",
		"format:ts": "bunx biome check --write ."
	},
	"type": "module",
	"dependencies": {
		"env-var": "^7.5.0",
		"envalid": "^8.0.0"
	}
}
